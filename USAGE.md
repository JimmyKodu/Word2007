# Word 2007 使用指南

## 概述

Word 2007 是一个基于 Electron 的桌面文字处理应用，提供文档查看和编辑功能。

## 安装和运行

### 1. 安装依赖

```bash
npm install
```

### 2. 启动应用

```bash
npm start
```

### 3. 验证安装

```bash
./validate.sh
```

## 功能说明

### 文档操作

#### 新建文档
- 点击工具栏 "📄 新建" 按钮
- 或使用快捷键 `Ctrl+N`

#### 打开文档
- 点击工具栏 "📂 打开" 按钮
- 或使用快捷键 `Ctrl+O`
- 支持的文件格式：
  - `.docx` - Microsoft Word 文档（使用 Mammoth.js 转换）
  - `.doc` - 旧版 Word 文档（作为文本读取）
  - `.txt` - 纯文本文件

#### 保存文档
- 点击工具栏 "💾 保存" 按钮
- 或使用快捷键 `Ctrl+S`
- 首次保存会提示选择保存位置

#### 另存为
- 使用菜单 `文件 > 另存为`
- 或使用快捷键 `Ctrl+Shift+S`

### 文本编辑

#### 基本编辑
- 在编辑区域直接输入文字
- 支持标准编辑操作：
  - 剪切：`Ctrl+X`
  - 复制：`Ctrl+C`
  - 粘贴：`Ctrl+V`
  - 全选：`Ctrl+A`
  - 撤销：`Ctrl+Z`
  - 重做：`Ctrl+Y`

#### 文本格式化

##### 粗体
- 点击工具栏 "**B**" 按钮
- 或使用快捷键 `Ctrl+B`

##### 斜体
- 点击工具栏 "_I_" 按钮
- 或使用快捷键 `Ctrl+I`

##### 下划线
- 点击工具栏 "<u>U</u>" 按钮
- 或使用快捷键 `Ctrl+U`

##### 字体大小
- 使用工具栏的字体大小下拉菜单
- 可选：12, 14, 16, 18, 20, 24, 28, 32

#### 文本对齐

- **左对齐**：点击 "≡" 按钮
- **居中对齐**：点击 "≣" 按钮
- **右对齐**：点击 "≢" 按钮

### 状态栏

底部状态栏显示：
- 左侧：当前文件名（未保存显示 "*"）
- 右侧：字符数和词数统计

## 界面说明

### 工具栏（从左到右）

1. **文件操作组**
   - 新建文档
   - 打开文档
   - 保存文档

2. **格式化组**
   - 粗体
   - 斜体
   - 下划线

3. **字体大小组**
   - 字体大小选择器

4. **对齐组**
   - 左对齐
   - 居中
   - 右对齐

### 菜单栏

#### 文件菜单
- 新建 (Ctrl+N)
- 打开 (Ctrl+O)
- 保存 (Ctrl+S)
- 另存为 (Ctrl+Shift+S)
- 退出 (Ctrl+Q)

#### 编辑菜单
- 撤销 (Ctrl+Z)
- 重做 (Ctrl+Y)
- 剪切 (Ctrl+X)
- 复制 (Ctrl+C)
- 粘贴 (Ctrl+V)
- 全选 (Ctrl+A)

#### 视图菜单
- 刷新 (Ctrl+R)
- 开发者工具 (Ctrl+Shift+I)
- 全屏

## 技术特性

### 架构
- **前端框架**：Electron
- **文档处理**：Mammoth.js（用于 .docx 文件）
- **编辑器**：ContentEditable HTML

### 文件格式支持

#### .docx 文件
- 使用 Mammoth.js 将 .docx 转换为 HTML
- 保留基本格式（粗体、斜体、下划线等）
- 保存为纯文本格式

#### .txt 文件
- 直接读取和保存
- 保留原始文本内容
- 换行符保持一致

#### .doc 文件
- 作为文本文件处理
- 可能丢失某些格式

### 实时功能
- **字数统计**：实时更新字符数和词数
- **修改标记**：文件名后显示 "*" 表示未保存
- **自动格式化**：支持富文本编辑

## 快捷键总览

| 功能 | Windows/Linux | macOS |
|------|--------------|-------|
| 新建 | Ctrl+N | Cmd+N |
| 打开 | Ctrl+O | Cmd+O |
| 保存 | Ctrl+S | Cmd+S |
| 另存为 | Ctrl+Shift+S | Cmd+Shift+S |
| 退出 | Ctrl+Q | Cmd+Q |
| 撤销 | Ctrl+Z | Cmd+Z |
| 重做 | Ctrl+Y | Cmd+Y |
| 剪切 | Ctrl+X | Cmd+X |
| 复制 | Ctrl+C | Cmd+C |
| 粘贴 | Ctrl+V | Cmd+V |
| 全选 | Ctrl+A | Cmd+A |
| 粗体 | Ctrl+B | Cmd+B |
| 斜体 | Ctrl+I | Cmd+I |
| 下划线 | Ctrl+U | Cmd+U |
| 刷新 | Ctrl+R | Cmd+R |
| 开发者工具 | Ctrl+Shift+I | Cmd+Shift+I |

## 常见问题

### Q: 为什么打开 .docx 文件后格式有变化？
A: Mammoth.js 将 .docx 转换为 HTML 时，会保留基本格式，但某些高级格式可能无法完全保留。

### Q: 保存的文件是什么格式？
A: 目前保存为纯文本格式（.txt），保留文本内容但不保留富文本格式。

### Q: 如何保留格式？
A: 在未来版本中将添加保存为 .docx 格式的功能。当前版本适合文本编辑和查看。

### Q: 支持图片吗？
A: 当前版本主要专注于文本编辑，暂不支持图片插入。

## 开发信息

### 项目结构

```
Word2007/
├── main.js          # Electron 主进程
├── renderer.js      # 渲染进程（UI 逻辑）
├── index.html       # 主界面 HTML
├── styles.css       # 样式表
├── package.json     # 项目配置
└── README.md        # 项目说明
```

### 依赖项
- `electron`: 桌面应用框架
- `mammoth`: .docx 文件处理

### 开发模式
启动应用后，按 `Ctrl+Shift+I` 打开开发者工具进行调试。

## 许可证

MIT License
